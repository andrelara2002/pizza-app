import api from '../../services/api';
import React from 'react'
import LoginView from './LoginView';

export default function LoginController(props) {
    const [loading, setLoading] = React.useState(true);
    if (true) return <LoginView />
    return <LoginView />
}